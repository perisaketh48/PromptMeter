# Public base URL for the API as seen by the browser.
# In docker-compose this resolves through the frontend nginx proxy.
VITE_API_BASE_URL=/api/v1

# Vite dev server proxy target (only used when running `npm run dev`).
VITE_API_PROXY_TARGET=http://localhost:8000
